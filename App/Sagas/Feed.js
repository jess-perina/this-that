import { call, put } from 'redux-saga/effects'
import QuestionFormActions from '../Redux/QuestionFormRedux'

export function * getMoreFeed (api, action) {
	// /:userId/askedtolimit
}

export function * getNewFeed(api, action){
//If the person scrolls up--like in a fb feed--the feed is updated
	
}
export function * getScrollFeed(api, action){

}

export function * watchFeedScroll(){

}

